services:
  linkedin-extractor:
    build: .
    container_name: linkedin-post-extractor
    restart: unless-stopped
    ports:
      - "8766:8766"
    environment:
      CHROME_CDP_ADDRESS: host.docker.internal:9222
    extra_hosts:
      - "host.docker.internal:host-gateway"
